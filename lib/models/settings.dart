/// TTS mode: system built-in, traditional cloud API, or LLM-based
enum TtsMode { system, traditional, llm }

/// OCR mode: on-device ML Kit or LLM multimodal vision
enum OcrMode { mlkit, llm }

/// App language
enum AppLocale { en, zh }
